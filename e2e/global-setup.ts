async function globalSetup() {
  console.log('Test setup: Using test database family-todos-test.db')
  console.log('Note: Database will be seeded with fresh data when server starts')
}

export default globalSetup
